# LoanSight Frontend

React + Vite frontend untuk aplikasi klasifikasi kelayakan pinjaman.

## Menjalankan

```bash
npm install
npm run dev
```

Frontend tersedia di `http://localhost:5173`.

Untuk mengganti URL backend:

```bash
VITE_API_BASE_URL=http://localhost:8000/api npm run dev
```
